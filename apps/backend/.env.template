# Database (provisioned by DevOps — see BIL-2)
DATABASE_URL=postgres://binchen:CHANGE_ME@localhost:5432/binchen_dev

# Redis (optional for dev, required for prod queues)
# REDIS_URL=redis://localhost:6379

# App URLs
MEDUSA_BACKEND_URL=http://localhost:9000
STORE_CORS=http://localhost:3000
ADMIN_CORS=http://localhost:7001
AUTH_CORS=http://localhost:3000,http://localhost:7001

# Secrets (rotate before production)
JWT_SECRET=CHANGE_ME
COOKIE_SECRET=CHANGE_ME

# Stripe (sandbox — real keys in DevOps vault)
STRIPE_API_KEY=sk_test_CHANGE_ME
STRIPE_WEBHOOK_SECRET=whsec_CHANGE_ME

# PayPal (sandbox — real keys in DevOps vault)
PAYPAL_CLIENT_ID=CHANGE_ME
PAYPAL_CLIENT_SECRET=CHANGE_ME
PAYPAL_AUTH_WEBHOOK_ID=CHANGE_ME

# Klarna (added Week 3 — not yet wired)
# KLARNA_API_KEY=CHANGE_ME
# KLARNA_API_SECRET=CHANGE_ME
